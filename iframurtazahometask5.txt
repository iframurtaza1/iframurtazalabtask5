LAB MANUAL 5


#include <iostream>
using namespace std;
int main() {
  //                             LAB TASK 01
  
/*int x;
do {
        cout << "Enter a number: ";
        cin >> x;
    }
while (x > 0);

cout << "You entered a non-positive number. Program terminated." << endl;

return 0;*/


//                          LAB TASK 03(A)
{
/*int num = 2,sum=0;

do {
    sum += num;
    num += 2;
}
 while (num <= 100);

    cout << "The sum of all even numbers between 2 and 100 is: " << sum << endl;

    return 0;
}*/
//                          LAB TASK 03(B)
/*int num = 1;
int sum = 0;

while (num <= 100) {
    sum += num * num;
    num++;
    }

cout << "The sum of the squars between 1 and 100 is: " << sum << endl;

return 0;*/
//                            LAB TASK 04(A)
// showing error 
//                            lab task 04 (b)
/*int a, b;
int sum = 0;
cout << "Enter the value of a(must be odd): ";
cin >> a;
cout << "Enter the value of b(must be odd): ";
cin >> b;

for (int i = a; i <= b; i++) {
    if (i % 2 != 0) {
      sum += i;
        }
    }
cout << "The sum of all odd numbers between " << a << " and " << b << " is " << sum << "." << endl;
return 0;











}